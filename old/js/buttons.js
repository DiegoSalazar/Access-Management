$(document).ready(function(){
	$('#mem_card').hover(
		function(){ // Change the input image's source when we "roll on"
			$(this).attr({ src : 'buttons/Member-card-button-over.png'});
		},
		function(){ // Change the input image's source back to the default on "roll off"
			$(this).attr({ src : 'buttons/Member-card-button.png'});             }
	);
	
	$('#reprint_card').hover(
		function(){ // Change the input image's source when we "roll on"
			$(this).attr({ src : 'buttons/reprint-card-button-over.png'});
		},
		function(){ // Change the input image's source back to the default on "roll off"
			$(this).attr({ src : 'buttons/reprint-card-button.png'});             }
	);
	
	$('#checkin').hover(
		function(){ // Change the input image's source when we "roll on"
			$(this).attr({ src : 'buttons/checkin-button-over.png'});
		},
		function(){ // Change the input image's source back to the default on "roll off"
			$(this).attr({ src : 'buttons/checkin-button.png'});             }
	);
	
	$('#newStaff').hover(
		function(){ // Change the input image's source when we "roll on"
			$(this).attr({ src : 'buttons/newStaff-button-over.png'});
		},
		function(){ // Change the input image's source back to the default on "roll off"
			$(this).attr({ src : 'buttons/newStaff-button.png'});             }
	);
	
	$('#deleteStaff').hover(
		function(){ // Change the input image's source when we "roll on"
			$(this).attr({ src : 'buttons/deleteStaff-button-over.png'});
		},
		function(){ // Change the input image's source back to the default on "roll off"
			$(this).attr({ src : 'buttons/deleteStaff-button.png'});             }
	);
	
	$('#entrance_log').hover(
		function(){ // Change the input image's source when we "roll on"
			$(this).attr({ src : 'buttons/entrance_log-button-over.png'});
		},
		function(){ // Change the input image's source back to the default on "roll off"
			$(this).attr({ src : 'buttons/entrance_log-button.png'});             }
	);
	
	$('#edit_slideshow').hover(
		function(){ // Change the input image's source when we "roll on"
			$(this).attr({ src : 'buttons/edit_slideshow-button-over.png'});
		},
		function(){ // Change the input image's source back to the default on "roll off"
			$(this).attr({ src : 'buttons/edit_slideshow-button.png'});             }
	);
	
	$('#allMembers').hover(
		function(){ // Change the input image's source when we "roll on"
			$(this).attr({ src : 'buttons/all_members-button-over.png'});
		},
		function(){ // Change the input image's source back to the default on "roll off"
			$(this).attr({ src : 'buttons/all_members-button.png'});             }
	);
	
	$('#all_singles').hover(
		function(){ // Change the input image's source when we "roll on"
			$(this).attr({ src : 'buttons/all_singles-button-over.png'});
		},
		function(){ // Change the input image's source back to the default on "roll off"
			$(this).attr({ src : 'buttons/all_singles-button.png'});             }
	);
	
	$('#all_couples').hover(
		function(){ // Change the input image's source when we "roll on"
			$(this).attr({ src : 'buttons/all_couples-button-over.png'});
		},
		function(){ // Change the input image's source back to the default on "roll off"
			$(this).attr({ src : 'buttons/all_couples-button.png'});             }
	);
	
	$('#member_scores').hover(
		function(){ // Change the input image's source when we "roll on"
			$(this).attr({ src : 'buttons/member_scores-button-over.png'});
		},
		function(){ // Change the input image's source back to the default on "roll off"
			$(this).attr({ src : 'buttons/member_scores-button.png'});             }
	);
	
	$('#delete_mem').hover(
		function(){ // Change the input image's source when we "roll on"
			$(this).attr({ src : 'buttons/deleteMember-button-over.png'});
		},
		function(){ // Change the input image's source back to the default on "roll off"
			$(this).attr({ src : 'buttons/deleteMember-button.png'});             }
	);
	
	$('#renew_mem').hover(
		function(){ // Change the input image's source when we "roll on"
			$(this).attr({ src : 'buttons/renew_membership-button-over.png'});
		},
		function(){ // Change the input image's source back to the default on "roll off"
			$(this).attr({ src : 'buttons/renew_membership-button.png'});             }
	);
	
	$('#get_mem_detail').hover(
		function(){ // Change the input image's source when we "roll on"
			$(this).attr({ src : 'buttons/mem_detail-button-over.png'});
		},
		function(){ // Change the input image's source back to the default on "roll off"
			$(this).attr({ src : 'buttons/mem_detail-button.png'});             }
	);
});
